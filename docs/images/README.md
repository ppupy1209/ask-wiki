# 이미지 (스크린샷) 저장 위치

포트폴리오·README에서 쓸 이미지를 여기에 둡니다.
아래 파일명으로 저장하면, README에 주석 처리해 둔 이미지 링크만 풀어 바로 보여줄 수 있습니다.

- `vthreads-threads-off-on.png` : 부하 중 JVM 라이브 스레드 수 그래프 (가상 스레드 OFF ~350 vs ON ~220)
- `grafana-dashboard.png` : "RAG 서비스 관측 대시보드" 전체 (부하 거는 중)

그 외 자유롭게 추가 가능 (예: `k6-off.png`, `k6-on.png`, `cache-hit.png`).
