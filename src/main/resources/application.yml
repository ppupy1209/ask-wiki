spring:
  application:
    name: ask-wiki
  servlet:
    multipart:
      max-file-size: 20MB      # PDF 업로드 대비 (기본 1MB)
      max-request-size: 20MB
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/ragdoc}
    username: ${DB_USERNAME:ragdoc}
    password: ${DB_PASSWORD:ragdoc}
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
  threads:
    virtual:
      enabled: ${SPRING_THREADS_VIRTUAL_ENABLED:false}
  ai:
    ollama:
      base-url: ${OLLAMA_BASE_URL:http://localhost:11434}
      embedding:
        options:
          model: nomic-embed-text
      chat:
        options:
          model: llama3.2:3b

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always
  metrics:
    tags:
      application: ask-wiki
    distribution:
      percentiles-histogram:
        http.server.requests: true   # p99 등 히스토그램 지표(_bucket) 생성 → Grafana에서 백분위 계산
